<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You</title>
    <style>
        :root {
            --gold: #C6A461;
            --dark-gold: #9E7D3E;
            --light-gold: #E8D9B5;
            --white: #ffffff;
            --black: #333333;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-image: linear-gradient(135deg, #f5f5f5 0%, #f0f0f0 100%);
        }

        .thank-you-container {
            text-align: center;
            max-width: 500px;
            padding: 40px;
            position: relative;
        }

        .confirmation-animation {
            width: 120px;
            height: 120px;
            margin: 0 auto 30px;
            position: relative;
        }

        .checkmark {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            display: block;
            stroke-width: 5;
            stroke: var(--gold);
            stroke-miterlimit: 10;
            box-shadow: 0 0 0 rgba(198, 164, 97, 0.4);
            animation: fill .4s ease-in-out .4s forwards, scale .3s ease-in-out .9s both;
            position: relative;
            background: rgba(198, 164, 97, 0.1);
        }

        .checkmark__circle {
            stroke-dasharray: 166;
            stroke-dashoffset: 166;
            stroke-width: 5;
            stroke-miterlimit: 10;
            stroke: var(--gold);
            fill: none;
            animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
        }

        .checkmark__check {
            transform-origin: 50% 50%;
            stroke-dasharray: 48;
            stroke-dashoffset: 48;
            animation: stroke 0.3s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
        }

        @keyframes stroke {
            100% {
                stroke-dashoffset: 0;
            }
        }

        @keyframes scale {
            0%, 100% {
                transform: none;
            }
            50% {
                transform: scale3d(1.1, 1.1, 1);
            }
        }

        @keyframes fill {
            100% {
                box-shadow: inset 0 0 0 100px rgba(198, 164, 97, 0.1);
            }
        }

        h1 {
            color: var(--dark-gold);
            margin-bottom: 20px;
            font-size: 32px;
            font-weight: 700;
        }

        p {
            color: var(--black);
            margin-bottom: 30px;
            font-size: 18px;
            line-height: 1.6;
        }

        .download-btn {
            background: linear-gradient(135deg, var(--dark-gold), var(--gold));
            color: white;
            border: none;
            padding: 16px 30px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(158, 125, 62, 0.3);
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            overflow: hidden;
            text-decoration: none;
        }

        .download-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(158, 125, 62, 0.4);
        }

        .download-btn:active {
            transform: translateY(1px);
        }

        .download-btn::after {
            content: "";
            position: absolute;
            top: 50%;
            left: 50%;
            width: 5px;
            height: 5px;
            background: rgba(255, 255, 255, 0.5);
            opacity: 0;
            border-radius: 100%;
            transform: scale(1, 1) translate(-50%);
            transform-origin: 50% 50%;
        }

        .download-btn:focus:not(:active)::after {
            animation: ripple 1s ease-out;
        }

        @keyframes ripple {
            0% {
                transform: scale(0, 0);
                opacity: 0.5;
            }
            100% {
                transform: scale(20, 20);
                opacity: 0;
            }
        }

        .gold-decoration {
            position: absolute;
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, var(--light-gold) 0%, rgba(232, 217, 181, 0) 70%);
            z-index: -1;
        }

        .decoration-1 {
            top: -50px;
            left: -50px;
        }

        .decoration-2 {
            bottom: -50px;
            right: -50px;
        }
        @media (max-width: 767px) {
        body {
        flex-direction: column;
        padding: 20px;
      }
    
      .thank-you-container {
        max-width: 90%;
        padding: 25px 20px;
      }
    
  .confirmation-animation {
    width: 80px;
    height: 80px;
    margin-bottom: 20px;
  }

  .checkmark {
    stroke-width: 4;
  }

  h1 {
    font-size: 24px;
    margin-bottom: 15px;
  }

  p {
    font-size: 16px;
    margin-bottom: 20px;
  }

  .download-btn {
    font-size: 16px;
    padding: 12px 24px;
    width: 100%;
    text-align: center;
    box-sizing: border-box;
  }

  .gold-decoration {
    width: 120px;
    height: 120px;
  }

  .decoration-1 {
    top: -30px;
    left: -30px;
  }

  .decoration-2 {
    bottom: -30px;
    right: -30px;
  }
}

    </style>
</head>
<body>
    <div class="container" id="container">
    <div class="thank-you-container">
        <div class="gold-decoration decoration-1"></div>
        <div class="gold-decoration decoration-2"></div>
        
        <div class="confirmation-animation">
            <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                <circle class="checkmark__circle" cx="26" cy="26" r="25" fill="none"/>
                <path class="checkmark__check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
            </svg>
        </div>
        
        <h1>Thank You!</h1>
        <p>Your details have been successfully submitted. Click below to download your file.</p>
        <a class="download-btn" id="download-btn" onclick="downloadFile()">Click Here to Download</a>
    </div>
</div>
    <script>
     
        function downloadFile() {
            console.log("Download button clicked"); 
            const downloadBtn = document.querySelector(".download-btn"); 
            const container = document.getElementById("container");
        
           
                downloadBtn.setAttribute("href", "golden-tower.pdf");
                downloadBtn.setAttribute("download", "SLV Brochure.pdf");
                setTimeout(() => {
                    window.location.href = "/"; 
                }, 1000);
                
        
        }
        
    </script>
</body>
</html>